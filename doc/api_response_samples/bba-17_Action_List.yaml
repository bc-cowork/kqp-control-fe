# wroted yaml format, but response format is JSON!


# BBA-17:   Action List

okay:     true
data:
  node:   prod1
  list:
    - name:         'action_log'
      path:         'cfg/actions/action_log.moon'
      url:          '{URL}'    # 클릭했을때 해당 Action Item Detail로 점프할 주소
      timestamp:    '2025-10-28 13:05:30'
      ref_layout:   3
      ref_process:  3
      desc:         '증적로그 기록'

